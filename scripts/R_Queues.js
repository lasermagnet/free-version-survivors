// Items (upgrades) and enemies to spawn at the beginning
export const Queues = {
    "Items": ["Myst", "Heal2"], // [] = don't spawn any items at the start (when enemies start dying)
    "Enemies": 20 // 0 = don't spawn enemies at the start
}