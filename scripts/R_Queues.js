// Items (upgrades) and enemies to spawn at the beginning
export const Queues = {
    "Items": ["Heal1", "Sword"], // [] = don't spawn any items at the start (when enemies start dying)
    "Enemies": 30 // 0 = don't spawn enemies at the start
}